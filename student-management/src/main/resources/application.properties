spring.application.name=student-management
server.port=8080
# Firebase project id (service account is still loaded via env var GOOGLE_APPLICATION_CREDENTIALS)
firebase.project-id=your-firebase-project-id